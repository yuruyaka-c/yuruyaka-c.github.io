# 第 21 章 画像処理

## 21.1

### 21.1.1

```c title="行ごとに別の配列を用意する"
#include <stdio.h>

int main()
{
	int row1[] = { 100, 200, 300, 400 };
	int row2[] = { 500, 600, 700, 800 };
	int row3[] = { 900, 1000, 1100, 1200 };

	printf("%d %d %d %d\n", row1[0], row1[1], row1[2], row1[3]);
	printf("%d %d %d %d\n", row2[0], row2[1], row2[2], row2[3]);
	printf("%d %d %d %d\n", row3[0], row3[1], row3[2], row3[3]);
}
```

### 21.1.2

```c title="一次元配列に二次元データを格納する"
#include <stdio.h>

int main()
{
	// 改行は見やすくするためのもの
	int arr[] = { 100, 200, 300, 400,
				  500, 600, 700, 800,
				  900, 1000, 1100, 1200 };

	printf("%d %d %d %d\n", arr[0], arr[1], arr[2], arr[3]);
	printf("%d %d %d %d\n", arr[4], arr[5], arr[6], arr[7]);
	printf("%d %d %d %d\n", arr[8], arr[9], arr[10], arr[11]);
}
```

### 21.1.3

```c title="行番号と列番号からインデックスを求める"
#include <stdio.h>
#define H 3
#define W 4

int main()
{
	int arr[W * H] = { 100, 200, 300, 400,
					   500, 600, 700, 800,
					   900, 1000, 1100, 1200 };

	for (int y = 0; y < H; ++y)
	{
		for (int x = 0; x < W; ++x)
		{
			printf("%d ", arr[y * W + x]);
		}

		putchar('\n');
	}
}
```

### 21.1.4

```c title="動的に二次元データ用の領域を確保する"
#include <stdio.h>
#include <stdlib.h>

int main()
{
	int H = 3;
	int W = 4;
	int* p = (int*)malloc(sizeof(int) * W * H);

	if (p == NULL)
	{
		puts("メモリの確保に失敗");
		return EXIT_FAILURE;
	}

	for (int i = 0; i < W * H; ++i)
	{
		p[i] = (i + 1) * 100;
	}

	for (int y = 0; y < H; ++y)
	{
		for (int x = 0; x < W; ++x)
		{
			printf("%d ", p[y * W + x]);
		}

		putchar('\n');
	}

	free(p);
}
```

## 21.2

### 21.2.1

```c title="BMP 形式の画像ファイルを出力する"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);
	Save("output.bmp", image);
	DestroyImage(image);
}
```

### 21.2.2

```c title="緑色に塗りつぶした画像を出力する"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);

	for (int y = 0; y < image->height; ++y)
	{
		for (int x = 0; x < image->width; ++x)
		{
			SetPixel(image, x, y, (RGB){ 0.0, 0.8, 0.4 });
		}
	}

	Save("green.bmp", image);
	DestroyImage(image);
}
```

### 21.2.3

```c title="グラデーションの画像を出力する"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);

	for (int y = 0; y < image->height; ++y)
	{
		for (int x = 0; x < image->width; ++x)
		{
			RGB color;
			color.r = (double)x / (image->width - 1);
			color.g = (double)y / (image->height - 1);
			color.b = 0.25;
			SetPixel(image, x, y, color);
		}
	}

	Save("gradient.bmp", image);
	DestroyImage(image);
}
```

## 21.3

### 21.3.1

```c title="線分を描く"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);

	for (int x = 0; x < 256; ++x)
	{
		SetPixel(image, x, 100, (RGB){ 0.0, 1.0, 0.0 });
	}

	for (int x = 100; x < 200; ++x)
	{
		SetPixel(image, x, 120, (RGB){ 0.0, 0.0, 1.0 });
	}

	Save("lines.bmp", image);
	DestroyImage(image);
}
```

### 21.3.2

```c title="長方形を描く"
// 省略: 21.2.1 画像プログラミングの共通コード

// 長方形を描く関数
void DrawRectangle(Image* image, int x, int y, int width, int height, RGB color)
{
	for (int y2 = y; y2 < (y + height); ++y2)
	{
		for (int x2 = x; x2 < (x + width); ++x2)
		{
			SetPixel(image, x2, y2, color);
		}
	}
}

int main()
{
	Image* image = CreateImage(256, 256);
	DrawRectangle(image, 50, 50, 120, 100, (RGB) { 0.0, 0.5, 0.5 });
	Save("rectangle.bmp", image);
	DestroyImage(image);
}
```

### 21.3.3

```c title="ブレゼンハムのアルゴリズム"
// 省略: 21.2.1 画像プログラミングの共通コード

// 線分を描く関数
void DrawLine(Image* image, int x0, int y0, int x1, int y1, RGB color)
{
	const int dx = abs(x1 - x0);
	const int dy = abs(y1 - y0);
	const int sx = (x0 < x1) ? 1 : -1;
	const int sy = (y0 < y1) ? 1 : -1;
	int x = x0;
	int y = y0;
	int error = (dx - dy);

	for (;;)
	{
		SetPixel(image, x, y, color);

		if ((x == x1) && (y == y1))
		{
			break;
		}

		const int e2 = (error * 2);

		if (e2 > -dy)
		{
			error -= dy;
			x += sx;
		}

		if (e2 < dx)
		{
			error += dx;
			y += sy;
		}
	}
}

int main()
{
	Image* image = CreateImage(256, 256);
	DrawLine(image, 50, 50, 200, 150, (RGB){ 0.0, 0.0, 0.0 });
	Save("bresenham.bmp", image);
	DestroyImage(image);
}
```

### 21.3.4

```c title="円を描く"
// 省略: 21.2.1 画像プログラミングの共通コード

// 円を描く関数
void DrawCircle(Image* image, int cx, int cy, int radius, RGB color)
{
	const int left = cx - radius;
	const int top = cy - radius;
	const int right = cx + radius;
	const int bottom = cy + radius;

	// 円をちょうど囲む正方形の範囲を対象にする
	for (int y = top; y <= bottom; ++y)
	{
		for (int x = left; x <= right; ++x)
		{
			const int dx = x - cx;
			const int dy = y - cy;

			// 「距離の二乗 <= 半径の二乗」なら円の内側
			if ((dx * dx + dy * dy) <= (radius * radius))
			{
				SetPixel(image, x, y, color);
			}
		}
	}
}

int main()
{
	Image* image = CreateImage(256, 256);
	DrawCircle(image, 128, 128, 50, (RGB){ 1.0, 0.6, 0.0 });
	Save("circle.bmp", image);
	DestroyImage(image);
}
```

## 追加のサンプルプログラム（Web 資料のおまけ）

### 1. 縦縞模様

```c title="縦縞模様"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("stripes1.bmp", image);
	DestroyImage(image);
}
```


### 2. 横縞模様

```c title="横縞模様"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("stripes2.bmp", image);
	DestroyImage(image);
}
```


### 3. 格子模様

```c title="格子模様"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("grid.bmp", image);
	DestroyImage(image);
}
```


### 4. 市松模様

```c title="市松模様"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("checker.bmp", image);
	DestroyImage(image);
}
```


### 5. 市松模様を円で切り抜き

```c title="市松模様を円で切り抜き"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("checker_circle.bmp", image);
	DestroyImage(image);
}
```


### 6. 水玉模様

```c title="水玉模様"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("polka_dots.bmp", image);
	DestroyImage(image);
}
```


### 7. 楕円

```c title="楕円"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("ellipse.bmp", image);
	DestroyImage(image);
}
```


### 8. 輪郭がなめらかな円

```c title="輪郭がなめらかな円"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("smooth_circle.bmp", image);
	DestroyImage(image);
}
```


### 9. ノイズ

```c title="ノイズ"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("noise.bmp", image);
	DestroyImage(image);
}
```


### 10. レンガ模様

```c title="レンガ模様"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("brick.bmp", image);
	DestroyImage(image);
}
```


### 11. 波紋

```c title="波紋"
// 省略: 21.2.1 画像プログラミングの共通コード

int main()
{
	Image* image = CreateImage(256, 256);


	Save("ripples.bmp", image);
	DestroyImage(image);
}
```

