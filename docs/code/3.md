# 第 3 章 変数とキーボードからの入力

## 3.2

### 3.2.1

```c title="変数を使わない場合のコード"
#include <stdio.h>

int main()
{
	printf("%.1f\n", ((80 + 100) / 2.0));
}
```

### 3.2.2

```c title="変数を使う場合のコード"
#include <stdio.h>

int main()
{
	int mathScore = 80;
	int englishScore = 100;
	double averageScore = (mathScore + englishScore) / 2.0;
	printf("%.1f\n", averageScore);
}
```

### 3.2.4

```c title="int 型の変数を定義するコード"
#include <stdio.h>

int main()
{
	// int 型の変数 price を定義する
	int price = 2200;
}
```

```c title="double 型の変数を定義するコード"
#include <stdio.h>

int main()
{
	// double 型の変数 distance を定義する
	double distance = 42.195;
}
```

### 3.2.5

```c title="int 型の変数を使うコード"
#include <stdio.h>

int main()
{
	int price = 2200; // 商品 1 個の価格
	printf("%d 円\n", price); // 1 個買ったときの金額を出力する
	printf("%d 円\n", (price * 10)); // 10 個買ったときの金額を出力する
}
```

```c title="double 型の変数を使うコード"
#include <stdio.h>

int main()
{
	double distance = 42.195; // マラソン 1 回の距離
	printf("%.3f km\n", distance); // マラソン 1 回の距離を出力する
	printf("%.3f km\n", (distance * 3)); // マラソン 3 回の距離を出力する
}
```

### 3.2.6

```c title="複数の変数を定義するコード"
#include <stdio.h>

int main()
{
	int mathScore = 80;
	int englishScore = 100;
	printf("合計点数: %d\n", (mathScore + englishScore));
}
```

```c title="❌ 誤り: 同じ名前の変数を2 回定義している"
#include <stdio.h>

int main()
{
	int score = 80;
	int score = 100; // コンパイルエラー
	printf("点数: %d\n", score);
}
```

### 3.2.7

```c title="1 行で複数の変数を定義するコード"
#include <stdio.h>

int main()
{
	int mathScore = 80, englishScore = 100;
	printf("合計点数: %d\n", (mathScore + englishScore));
}
```

### 3.2.8

```c title="将来の年齢を求めるコード①"
#include <stdio.h>

int main()
{
	int age = 19;
	printf("5 年後: %d\n", (age + 5));
	printf("10 年後: %d\n", (age + 10));
}
```

```c title="将来の年齢を求めるコード②"
#include <stdio.h>

int main()
{
	int age = 19;
	int age5 = age + 5;
	int age10 = age + 10;
	printf("5 年後: %d\n", age5);
	printf("10 年後: %d\n", age10);
}
```

```c title="将来の年齢を求めるコード③"
#include <stdio.h>

int main()
{
	int age = 19, age5 = (age + 5), age10 = (age + 10);
	printf("5 年後: %d\n", age5);
	printf("10 年後: %d\n", age10);
}
```

```c title="長方形の面積を求めるコード"
#include <stdio.h>

int main()
{
	int width = 20;
	int height = 10;
	int area = width * height;
	printf("%d\n", area);
}
```

```c title="円の面積を求めるコード"
#include <stdio.h>

int main()
{
	double radius = 1.1;
	double pi = 3.14;
	double area = radius * radius * pi;
	printf("%.2f\n", area);
}
```

## 3.4

### 3.4.1

```c title="変数の値を変更するコード"
#include <stdio.h>

int main()
{
	int price = 2200; // int 型の変数を定義
	printf("%d 円\n", price);

	price = 2500; // 変数の値を変更①
	printf("%d 円\n", price);

	price = 2500 + 500; // 変数の値を変更②
	printf("%d 円\n", price);
}
```

### 3.4.2

```c title="半額を計算するコード（正しくない）"
#include <stdio.h>

int main()
{
	int price = 2200;
	int halfPrice = price / 2;
	printf("%d 円\n", halfPrice);

	price = 3000;
	printf("%d 円\n", halfPrice);
}
```

```c title="半額を計算するコード"
#include <stdio.h>

int main()
{
	int price = 2200;
	int halfPrice = price / 2;
	printf("%d 円\n", halfPrice);

	price = 3000;
	halfPrice = price / 2;
	printf("%d 円\n", halfPrice);
}
```

### 3.4.3

```c title="自己参照的な代入を行うコード"
#include <stdio.h>

int main()
{
	int n = 5;
	n = n * 10 + 1;
	printf("%d\n", n);
}
```

### 3.4.4

```c title="❌ 誤り: 不定値の変数の値を出力するコード"
#include <stdio.h>

int main()
{
	int price;
	printf("%d 円\n", price);
}
```

```c title="修正したコード"
#include <stdio.h>

int main()
{
	int price;
	price = 2200;
	printf("%d 円\n", price);
}
```

### 3.4.5

```c title="💣️ 不定値の変数を使っているコード"
#include <stdio.h>

int main()
{
	int mathScore = 80;
	int englishScore;
	printf("Math: %d\n", mathScore);
	printf("English: %d\n", englishScore);
}
```

```c title="💣️ 不定値の変数を使っているコード"
#include <stdio.h>

int main()
{
	int a, b = 20, c;
	c = 30;
	printf("%d\n", c);
	printf("%d\n", (a + b + c));
}
```

```c title="💣️ 不定値の変数を使っているコード"
#include <stdio.h>

int main()
{
	int age;
	int age5 = age + 5;
	int age10 = age + 10;
	printf("5 年後: %d\n", age5);
	printf("10 年後: %d\n", age10);
}
```

## 3.5

```c title="例: 将来の年齢を求めるコード"
#include <stdio.h>

int main()
{
	// 現在の年齢
	int age = 19;

	printf("5 年後: %d\n", (age + 5));
	printf("10 年後: %d\n", (age + 10));
}
```

### 3.5.1

```c title="標準入力からの入力を活用するコード"
#include <stdio.h>

int main()
{
	printf("現在の年齢を入力してください > ");
	int age = 0;
	scanf("%d", &age);

	printf("5 年後: %d\n", (age + 5));
	printf("10 年後: %d\n", (age + 10));
}
```

### 3.5.2

```c title="標準入力から入力した「分」を「秒」に変換するコード"
#include <stdio.h>

int main()
{
	printf("分を入力してください > ");
	int minutes = 0;
	scanf("%d", &minutes);

	int seconds = minutes * 60;
	printf("%d 分は %d 秒です\n", minutes, seconds);
}
```

### 3.5.3

```c title="scanf で値がすぐに代入される変数の初期値を省略するコード"
#include <stdio.h>

int main()
{
	printf("分を入力してください > ");
	int minutes; // 直後に scanf で値を代入するため初期値を省略してよい
	scanf("%d", &minutes);

	int seconds = minutes * 60;
	printf("%d 分は %d 秒です\n", minutes, seconds);
}
```

### 3.5.4

```c title="標準入力から入力した円の半径から面積を求めるコード"
#include <stdio.h>

int main()
{
	printf("半径を入力してください > ");
	double radius;
	scanf("%lf", &radius);

	double pi = 3.14;
	double area = radius * radius * pi;
	printf("%.2f\n", area);
}
```

### 3.5.5

```c title="時と分を標準入力から読み込むコード"
#include <stdio.h>

int main()
{
	printf("時と分を入力してください > ");
	int hour;
	scanf("%d", &hour);

	int minute;
	scanf("%d", &minute);

	printf("%02d:%02d\n", hour, minute);
}
```

### 3.5.6

```c title="時と分を標準入力から読み込むコード"
#include <stdio.h>

int main()
{
	printf("時と分を入力してください > ");
	int hour, minute;
	scanf("%d %d", &hour, &minute);

	printf("%02d:%02d\n", hour, minute);
}
```

### 3.5.7

```c title="年齢、身長、体重を標準入力から読み込むコード"
#include <stdio.h>

int main()
{
	printf("年齢、身長（cm）、体重（kg）を入力してください > ");
	int age;
	double height, weight;
	scanf("%d %lf %lf", &age, &height, &weight);

	printf("年齢: %d\n", age);
	printf("身長: %.1f cm\n", height);
	printf("体重: %.1f kg\n", weight);
}
```

```c title="scanf のフォーマット文字列に、書式指定子・空白以外を使う例"
#include <stdio.h>

int main()
{
	printf("時と分を入力してください > ");
	int hour, minute;
	scanf("%d:%d", &hour, &minute);

	printf("%02d:%02d\n", hour, minute);
}
```

## 3.6

### 3.6.1

```c title="暗黙的な型変換の例①"
#include <stdio.h>

int main()
{
	printf("%f\n", (1.5 + 2));
	printf("%f\n", (1 + 0.5));
}
```

```c title="暗黙的な型変換の例②"
#include <stdio.h>

int main()
{
	int a = 2;
	printf("%f\n", (1.5 + a));
}
```

### 3.6.2

```c title="暗黙的な型変換の例（double 型に int 型の代入）①"
#include <stdio.h>

int main()
{
	double a = 10;
	printf("%f\n", a);
}
```

```c title="暗黙的な型変換の例（double 型に int 型の代入）②"
#include <stdio.h>

int main()
{
	int a = 20;
	double b = a;
	printf("%d\n", a);
	printf("%f\n", b);
}
```

```c title="暗黙的な型変換の例（double 型に int どうしの計算結果を代入）"
#include <stdio.h>

int main()
{
	int a = 5;
	double b = a / 2;
	printf("%d\n", a);
	printf("%f\n", b);
}
```

### 3.6.3

```c title="縮小変換が生じるコード①"
#include <stdio.h>

int main()
{
	double a = 12.345;
	int b = a; // 小数点以下は切り捨て
	printf("%f\n", a);
	printf("%d\n", b);
}
```

```c title="縮小変換が生じるコード②"
#include <stdio.h>

int main()
{
	int a = 3;
	int b = a / 2.0; // b への代入時に縮小変換
	printf("%d\n", b);
}
```

### 3.6.4

```c title="消費税込みの値段を計算するコード"
#include <stdio.h>

int main()
{
	int price = 101;
	double totalPrice = price * 1.1;
	printf("%f 円\n", totalPrice);
}
```

```c title="消費税込みの値段を計算するコード"
#include <stdio.h>

int main()
{
	int price = 101;
	int totalPrice = price * 1.1;
	printf("%d 円\n", totalPrice);
}
```

## 3.7

### 3.7.1

```c title="消費税込みの価格を計算するコード（キャストの使用）"
#include <stdio.h>

int main()
{
	int price = 101;
	int totalPrice = (int)(price * 1.1);
	printf("%d 円\n", totalPrice);
}
```

```c title="平均点を正確に計算するコード"
#include <stdio.h>

int main()
{
	int mathScore = 80;
	int englishScore = 90;
	int scienceScore = 90;

	// 合計点
	int totalScore = mathScore + englishScore + scienceScore;
	// 科目数
	int numSubjects = 3;
	// 平均点
	double averageScore = (double)totalScore / numSubjects;
	printf("平均点: %.2f\n", averageScore);
}
```

```c title="❌ 誤り: キャストの位置が間違っている"
#include <stdio.h>

int main()
{
	int mathScore = 80;
	int englishScore = 90;
	int scienceScore = 90;

	// 合計点
	int totalScore = mathScore + englishScore + scienceScore;
	// 科目数
	int numSubjects = 3;
	// 平均点
	double averageScore = (double)(totalScore / numSubjects);
	printf("平均点: %.2f\n", averageScore);
}
```

### 3.7.2

```c title="小数第 3 位を四捨五入するコード"
#include <stdio.h>

int main()
{
	double a = 1.66666666;
	printf("%.2f\n", a);
}
```

```c title="小数第 3 位以下を切り捨てるコード"
#include <stdio.h>

int main()
{
	double a = 1.66666666;
	int b = (int)(a * 100); // 166
	double c = b / 100.0; // 1.66

	printf("%.2f\n", c);
}
```

## 3.8

### 3.8.1

```c title="おつりを計算するコード"
#include <stdio.h>

int main()
{
	printf("商品の価格（円）を入力してください > ");
	int price;
	scanf("%d", &price);

	printf("支払った金額（円）を入力してください > ");
	int payment;
	scanf("%d", &payment);

	// おつりを計算して出力する
	int change = payment - price;
	printf("おつり: %d 円\n", change);
}
```

```c title="（参考）支払い不足の場合のエラー出力を追加したコード"
#include <stdio.h>

int main()
{
	printf("商品の価格（円）を入力してください > ");
	int price;
	scanf("%d", &price);

	printf("支払った金額（円）を入力してください > ");
	int payment;
	scanf("%d", &payment);

	// おつりを計算する
	int change = payment - price;

	if (change < 0)
	{
		puts("支払金額が不足しています");
	}
	else
	{
		// おつりを出力する
		printf("おつり: %d 円\n", change);
	}
}
```

### 3.8.2

```c title="日本円を米ドルに変換するコード"
#include <stdio.h>

int main()
{
	printf("日本円の金額を入力してください > ");
	int jpy;
	scanf("%d", &jpy);

	printf("1 米ドル何円であるかを入力してください > ");
	double jpyPerUsd;
	scanf("%lf", &jpyPerUsd);

	// 日本円を米ドルに変換して出力する
	double usd = jpy / jpyPerUsd;
	printf("USD: %.2f\n", usd);
}
```

### 3.8.3

```c title="華氏温度を摂氏温度に変換するコード"
#include <stdio.h>

int main()
{
	printf("華氏温度を入力してください > ");
	double fahrenheit;
	scanf("%lf", &fahrenheit);

	// 華氏温度を摂氏温度に変換して出力する
	double celsius = (fahrenheit - 32.0) * 5.0 / 9.0;
	printf("摂氏温度: %.2f\n", celsius);
}
```

### 3.8.4

```c title="BMI を計算するコード"
#include <stdio.h>

int main()
{
	printf("身長（cm）を入力してください > ");
	double cm;
	scanf("%lf", &cm);

	printf("体重（kg）を入力してください > ");
	double kg;
	scanf("%lf", &kg);

	// 身長をメートルに変換する
	double m = cm / 100.0;

	// BMI を計算して出力する
	double bmi = kg / (m * m);
	printf("BMI: %.2f\n", bmi);
}
```

### 3.8.5

```c title="秒を時分秒に変換するコード"
#include <stdio.h>

int main()
{
	printf("秒数を入力してください > ");
	int totalSeconds;
	scanf("%d", &totalSeconds);

	// 時間を計算する
	int hours = totalSeconds / 3600;

	// 分を計算する
	int minutes = (totalSeconds % 3600) / 60;

	// 秒を計算する
	int seconds = totalSeconds % 60;

	// 結果を出力する
	printf("%02d:%02d:%02d\n", hours, minutes, seconds);
}
```

### 3.8.6

```c title="税込みの価格を計算するコード"
#include <stdio.h>

int main()
{
	printf("税抜き価格（円）を入力してください > ");
	int price;
	scanf("%d", &price);

	printf("消費税率（%%）を入力してください > ");
	double taxRate;
	scanf("%lf", &taxRate);

	// 税込みの価格を計算する（int 型へのキャストで小数点以下を切り捨てる）
	int totalPrice = (int)(price * (1 + taxRate / 100));

	// 結果を出力する
	printf("税込み価格: %d 円\n", totalPrice);
}
```

## 3.9

### 3.9.1

```c title="main 関数内の複合文"
#include <stdio.h>

int main()
{
	puts("Apple");
	puts("Banana");
}
```

### 3.9.2

```c title="複合文のネスト"
#include <stdio.h>

int main()
{
	{
		puts("Apple");
		puts("Banana");

		{
			puts("Cat");
			puts("Dog");
		}
	}

	{
		puts("English");
	}
}
```

```c title="❌ インデントや空白行の使い方が適切でない"
#include <stdio.h>

int main()
{
	{
	puts("Apple");
	puts("Banana");
	{
	puts("Cat");
	puts("Dog");
	}
	}
	{
	puts("English");
	}
}
```

### 3.9.3

```c title="空の複合文の例"
#include <stdio.h>

int main()
{
	{

	}   // 空の複合文

	{
		puts("Cat");
		puts("Dog");
	}

	{
	}   // これも空の複合文
}
```

## 3.10

### 3.10.1

```c title="変数のスコープ"
#include <stdio.h>

int main()
{
	int a = 10;

	{
		int b = 20;

		{
			int c = 30;
		} // c のスコープが終わる
	} // b のスコープが終わる
} // a のスコープが終わる
```

```c title="❌ 誤り: スコープ外で変数を使っている"
#include <stdio.h>

int main()
{
	{
		int a = 10;
		printf("%d\n", a);
	}

	printf("%d\n", a); // エラー： a のスコープ外
}
```

### 3.10.2

```c title="多くの変数名が登場する例"
#include <stdio.h>

int main()
{
	{
		int a, b, c;
		scanf("%d %d %d", &a, &b, &c);
		printf("%d\n", (a + b + c));
	}

	{
		int d, e, f;
		scanf("%d %d %d", &d, &e, &f);
		printf("%d\n", (d * e * f));
	}
}
```

```c title="スコープを利用して変数名を再利用する例"
#include <stdio.h>

int main()
{
	{
		int a, b, c;
		scanf("%d %d %d", &a, &b, &c);
		printf("%d\n", (a + b + c));
	}

	{
		int a, b, c;
		scanf("%d %d %d", &a, &b, &c);
		printf("%d\n", (a * b * c));
	}
}
```

### 3.10.3

```c title="❌ 誤り: 変数のスコープを広くしたことで起こりうるミスの例"
#include <stdio.h>

int main()
{
	int a, b, c;
	int d, e, f;

	scanf("%d %d %d", &a, &d, &c); // b を使うつもりが d を使ってしまった
	printf("%d\n", (a + d + c)); // b を使うつもりが d を使ってしまった

	scanf("%d %d %d", &b, &e, &f); // d を使うつもりが b を使ってしまった
	printf("%d\n", (a * b * c)); // d, e, f を使うつもりが a, b, c を使ってしまった
}
```

## 3.11

### 3.11.1

```c title="シャドーイングの例"
#include <stdio.h>

int main()
{
	int n = 10;
	printf("%d\n", n); // 10

	{
		int n = 20;
		printf("%d\n", n); // 20

		{
			double n = 30;
			printf("%.1f\n", n); // 30.0
		}

		printf("%d\n", n); // 20
	}

	printf("%d\n", n); // 10
}
```
